# ── Web framework ────────────────────────────────────────────────────────────
Django>=4.2,<5.0
djangorestframework>=3.14,<3.16
djangorestframework-simplejwt>=5.3,<5.4
django-cors-headers>=4.3,<5.0
django-environ>=0.11

# ── Database ──────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9,<3.0
django-extensions>=3.2

# ── PostGIS / Geo ─────────────────────────────────────────────────────────────
# Uncomment when PostGIS is available in your PostgreSQL instance
# djangorestframework-gis>=1.0

# ── Background tasks ─────────────────────────────────────────────────────────
celery>=5.3,<6.0
redis>=5.0,<6.0
django-celery-beat>=2.5
django-celery-results>=2.5

# ── Data / ML ─────────────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
statsmodels>=0.14
xgboost>=2.0

# ── API / tooling ─────────────────────────────────────────────────────────────
drf-spectacular>=0.27
django-filter>=23.0
Pillow>=10.0

# ── IBM / AI ──────────────────────────────────────────────────────────────────
ibm-watsonx-ai>=1.0
requests>=2.31

# ── Security / utils ─────────────────────────────────────────────────────────
django-ratelimit>=4.0
python-decouple>=3.8
gunicorn>=22.0
whitenoise>=6.5

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest>=8.0
pytest-django>=4.8
factory-boy>=3.3
faker>=26.0
coverage>=7.5
